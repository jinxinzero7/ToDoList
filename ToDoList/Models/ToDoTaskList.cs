using ToDoList.Models;

public class ToDoTaskList
{
    /*
    private static ToDoTaskList _instance;
    private static readonly object _lock = new object();

    public List<ToDoItem> Tasks { get; set; } = new List<ToDoItem>();

    private ToDoTaskList() { } // Private constructor to prevent direct instantiation

    public static ToDoTaskList GetInstance()
    {
        if (_instance == null)
        {
            lock (_lock)
            {
                if (_instance == null)
                {
                    _instance = new ToDoTaskList();
                }
            }
        }
        return _instance;
    }
    */
}